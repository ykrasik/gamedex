rootProject.name = 'gamedex'

include "gamedex-common"

include "gamedex-persistence"

include "gamedex-data-provider"
include "gamedex-data-provider-giantbomb"

include "gamedex-app"

// All projects must be defined above this line, this must be the last piece of code.
rootProject.children.each { project ->
    project.buildFileName = "${project.name}.gradle"
}